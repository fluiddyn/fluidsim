[build-system]
# Further build requirements - like pythran and mpi4py - come from setup.py via
# the PEP 517 interface.
requires = ["setuptools>=49.5.0", "setuptools_scm", "wheel", "numpy", "transonic>=0.4.3"]
build-backend = "setuptools.build_meta"
