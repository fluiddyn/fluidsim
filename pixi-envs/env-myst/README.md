# An environment to use Myst, Typst and default Fluidsim

Typical usage: use Myst and Typst to produce a pdf from a .md notebook.

Install with

```sh
pixi run install-fluidsim
```

Use with

```sh
pixi shell
```

Check fluidsim import with

```sh
pixi run python -c "import fluidsim"
```
